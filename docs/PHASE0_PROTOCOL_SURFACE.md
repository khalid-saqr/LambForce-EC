# Phase 0 protocol-surface closure

Phase 0 closes the mechanics configuration, structural classes, controls, output definitions,
decision gates, and fail-closed provenance behavior. It does not establish a six-artery hydrodynamic
result.

The earlier description of Phase 1 as immutable-archive ingestion is superseded. The publication's
primary executable source is the exact `picoNewton_v2.ipynb` Git blob, so Phase 1 is an independent,
all-six published-source reproduction and regression workflow.

The mechanics protocol remains unchanged:

- ten load and sign-control cases;
- three spectral controls;
- three force-conserving load distributions;
- periodic, compliant-edge, and clamped-edge supports;
- bonded and tangential-slip limits;
- homogeneous and stiff-nuclear representations;
- elastic primary analysis before viscoelastic robustness;
- zero prestress only;
- full 36-state structural Cartesian product.

Claim-bearing mechanics remain blocked until Phase 1 freezes twelve qualified records: six arteries
in `historical_v2` and `verified` modes.
